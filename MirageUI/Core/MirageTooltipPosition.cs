namespace MirageUI;

public enum MirageTooltipPosition
{
    /// <summary>マウスカーソルに追従（既定）。</summary>
    Cursor,

    /// <summary>アンカー（ボタン等）の上。</summary>
    Above,

    /// <summary>アンカー（ボタン等）の下。</summary>
    Below,
}
