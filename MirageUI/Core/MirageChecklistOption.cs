namespace MirageUI;

/// <summary>Option row for checklist / filter popups.</summary>
public sealed class MirageChecklistOption
{
    public required string Id { get; init; }
    public required string Label { get; init; }
}
